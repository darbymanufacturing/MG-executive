# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Environment variables (contains API keys — never commit)
.env
.env.local
.env.*.local

.vercel

# Firebase Emulator persisted data + CLI cache
emulator-data/
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# On-demand Firestore backups (contain ALL prod data — keep off git; copy elsewhere for durability)
backups/

# Local plaintext secrets — never commit (canonical copies live in Vercel env)
secrets/
